<?php
/**
 * Fichier de définition des routes de l'application
 * 
 * -Chaque ligne du tableau $routes défini une route
 * -Chaque route est définie par
 *          -Le nom de la route
 *          -Le path
 *          -Le controller, la fonction déclenchée par la route
 *          -La/les méthodes
 */
$routes =[
    
];